# 软件工程作业系统产品需求文档

## 1. 文档概览

| 项目 | 内容 |
|---|---|
| 产品暂定名 | 软件工程作业系统 |
| 版本 | V1.0 |
| 状态 | 需求初稿，待师生评审 |
| 创建日期 | 2026-09-14 |
| 目标用户 | 高校教师、学生 |
| 产品形态 | Web 端，适配电脑与手机浏览器 |

## 2. 背景与目标

### 2.1 业务背景

目前教师通过 QQ 群发送作业压缩包，学生完成后再压缩并通过 QQ 提交。这种方式存在以下问题：

- 作业通知、要求、附件和学生提交通常混在聊天记录中，查找困难。
- 教师需要手工核对提交状态、文件版本和小组成员，统计成本高。
- 学生难以确认最新要求、截止时间和最终提交版本。
- 小组建立、选题、邀请和加入缺少统一流程，容易重复加入或产生争议。
- 小组成员提交的个人作品缺少统一的组内互评入口，评分与评语难以汇总和追踪。
- 文档需要反复下载、解压后才能查看，使用体验较差。

### 2.2 产品目标

建立一个以“教学班”为管理边界的作业平台。当前系统固定服务于“软件工程”课程，教师按学期创建教学班，完成名单导入、作业发布、提交管理、组内互评与成绩汇总；学生能够加入教学班、创建或加入小组、选题、协作提交并评价本组成员。

首版达成以下可验证目标（具体目标值通过试用和可用性测试确定）：

- 教师可以在较少步骤内完成一次含附件作业的发布。
- 学生可以在清晰引导下完成组队或入组申请。
- 每份小组作业只有一个清晰、可追溯的当前提交版本。
- 教师可以直接查看全班提交率、互评参与情况及成绩汇总。
- 支持 Markdown、PDF、常见图片和 Office 文档的在线预览或转码预览。

### 2.3 产品成功指标

| 指标 | 定义 | 首版目标 |
|---|---|---|
| 按时提交率 | 截止时间前完成提交的小组数 / 应提交小组数 | 试运行后统计，作为观察指标 |
| 提交状态准确率 | 系统统计与教师人工核验一致的提交数 / 总提交数 | 通过抽查验证，目标值待评审 |
| 互评参与率 | 至少提交过一份有效评价的学生数 / 可参与互评学生数 | 试运行后统计，作为观察指标 |
| 核心任务成功率 | 用户不求助即可完成发布、组队或提交 | 通过可用性测试验证，目标值待评审 |

## 3. 用户与权限

### 3.1 用户角色

| 角色 | 核心诉求 | 主要权限 |
|---|---|---|
| 教师 | 高效发布、收集、批改和统计作业 | 创建教学班、导入学生名单、管理成员与小组、发布作业、设置组内互评、评分、导出数据 |
| 学生组长 | 组织成员、确定选题、完成小组提交 | 创建小组、处理入组申请、邀请成员、填写本组选题、正式提交小组作业 |
| 学生组员 | 加入合适小组、参与提交和组内评价 | 申请或接受邀请、查看小组资料、上传提交、评价本组其他成员 |
| 系统管理员 | 保障系统正常运行 | 用户、课程、存储、违规内容及运行配置管理；不参与教学评分 |

### 3.2 权限原则

- 用户可同时加入不同学期或班级的教学班，但在同一教学班内只能加入一个小组。
- 小组作业允许成员共同上传草稿文件，但只有组长可以正式提交、撤回和重新提交；个人作业只能由学生本人提交。
- 同一小组成员能够查看本组全部提交版本，并在教师开启互评后评价本组其他成员，但不能评价自己。
- 学生不能查看其他小组的作业文件或不属于自己的组内互评内容，除非教师开启优秀作品公开展示；本人收到的评价按互评公开规则查看。
- 教师拥有本课程数据管理权；管理员仅因运维需要访问，并记录审计日志。

## 4. 范围与优先级

### 4.1 MVP（Must）

- 账号登录、角色与课程权限。
- 教师创建“软件工程”教学班，可导入学生名单并批量创建默认账号。
- 小组创建、申请加入、组长审批、成员邀请及受邀人确认。
- 同一教学班只能加入一个小组，支持主动退出与组长移交。
- 组长填写本组选题、教学班内选题查重及教师审核。
- 作业发布、附件、截止时间、提交对象及迟交规则。
- 小组作业多成员上传、版本记录、正式提交和撤回。
- 教师查看、在线预览、下载、评分和评语。
- 组内互评、评分量表、评语、截止时间和公开范围配置。
- 提交率、互评参与情况、成绩汇总及表格导出。

### 4.2 增强项（Should）

- Markdown、PDF、图片、DOCX、PPTX、XLSX 在线预览。
- 站内消息与待办提醒。
- 教师批量下载全部提交，按“作业-小组”自动命名。
- 小组成员操作动态与提交历史。
- 教师处理异常评价和成绩调整。

### 4.3 可选加分项（Could）

- 邮件、QQ 机器人或企业微信通知。
- 作业公开展示与优秀作品。
- 文档查重、代码仓库链接、自动化测试。
- 教师评分维度统计和课程数据看板。
- 对接学校统一身份认证。

### 4.4 本期不做（Won't）

- 多人实时在线编辑 Office 文档。
- 视频直播、签到、课堂互动等完整课堂平台功能。
- 教务系统排课、学分和正式成绩同步。
- 自研 Office 文档渲染引擎；采用第三方预览或服务端转 PDF。

## 5. 核心业务规则

### 5.1 教学班与账号规则

1. 当前系统的课程固定为“软件工程”；教师创建的是某学期、某班级的教学班，例如“2026 秋季-软件工程 1 班”。
2. 教师可通过 XLSX/CSV 模板导入学生名单，必填学号和姓名，可选班级、邮箱等信息。
3. 学号作为默认登录账号和初始密码。导入确认页明确提示“将使用学号作为初始密码”，由教师确认后执行导入，不显示或回显具体密码。
4. 系统不显示首次修改密码提示；学生可在个人菜单主动修改密码，教师可将密码重置为学号。
5. 初始密码和修改后的密码均只保存加盐哈希，不在名单、导出文件或日志中回显明文。
6. 若学号已存在，系统将该用户加入当前教学班而不重复创建账号，也不覆盖其现有密码；名单中的重复学号、空值和格式错误必须在导入预览中提示。
7. 导入采用“预览校验 -> 确认使用学号作为初始密码 -> 执行导入”，并反馈成功、新建、已存在、失败数量及失败原因。
8. 首版不开放学生自助注册或通过邀请码入班；遗漏学生必须由教师补充导入名单。
9. 教学班归档后只读，不再允许组队、提交或互评。

### 5.2 小组与选题规则

1. 学生未入组时可创建小组，也可申请加入多个小组；任一申请通过后，其他待处理申请自动失效。
2. 组长邀请学生时，对方必须确认；若对方已入组，邀请不可接受。
3. 小组人数必须满足教师设置的最小值与最大值。
4. 组长退出前必须移交组长；小组仅剩一人时可解散。
5. 截止组队后，学生不能自行加入、退出或解散，需由教师调整。
6. 组长在“我的小组”中填写本组选题；是否向学生展示公共选题列表由教师配置，默认不展示。
7. 同一教学班内，各小组选题原则上不得重复。系统按统一的名称规范化规则进行查重，具体规则待评审；若重复，只提示“该选题已被使用”，不向学生展示其他小组题目。
8. 教师可查看教学班全部小组及选题，并审核、驳回或要求修改；每个小组同一时间只有一个有效选题。
9. 组长修改已通过的选题需重新提交教师审核，原选题在新选题通过前继续保留，避免并发占题。

### 5.3 作业与提交规则

1. 教师发布作业时必须选择“个人作业”或“小组作业”，发布后已有提交时不能切换提交类型。
2. 教师设置标题、说明、附件、起止时间、提交格式、文件数量和单文件大小限制。
3. 小组作业中，成员上传的文件进入本组草稿区并记录上传人和时间，只有组长可以正式提交、撤回和重新提交。
4. 个人作业由学生本人上传、正式提交、撤回和重新提交，其他学生及其组长均无权代交。
5. 正式提交生成不可覆盖的版本快照；截止前可撤回并重新提交，历史版本保留。
6. 截止后是否允许迟交由教师设置；迟交必须醒目标记提交时间。
7. 截止时存在草稿但未正式提交，按“未提交”处理。
8. 成员变化不改变历史提交中记录的成员快照。

### 5.4 互评和成绩规则

1. 教师为已发布的个人作业开启组内作品互评，并设置评价维度、权重、评语要求和截止时间；小组作业不能创建互评活动。
2. 学生从本组除自己以外的成员中选择其最新正式提交版本进行评价；未提交作品只显示状态且不可评分。
3. 教师可增删评分维度并配置权重，权重合计必须为 100；界面实时计算总分，服务端复算后保存。
4. 互评公开范围由教师配置，默认被评价者可以看到评价人的姓名、分数和评语，其他普通学生不能查看，教师可查看全部评价。
5. 同一互评活动中，一名学生对同一成员只能保留一份有效评价；截止前是否允许修改或删除由教师配置。
6. 教师可查看参与率、每人评价数、每人获评数、组内评分分布和异常离群值，可作废不当评价并记录原因。
7. 由于评价对象由学生自由选择，可能出现获评数量不同。默认互评仅作为教师评分参考，不直接计入个人成绩；若教师开启成绩换算，系统必须展示样本数量并由教师确认计算结果。
8. 成绩在发布前仅教师可见；互评内容按教师配置的公开时间和范围展示，默认互评提交后被评价者可在“收到的评价”中查看评价人、分数和评语。

## 6. 核心流程

### 6.1 教师教学流程

创建教学班并导入名单 -> 发布组队与选题规则 -> 学生完成组队 -> 发布作业 -> 查看提交进度 -> 截止作业 -> 开启组内互评 -> 教师评分 -> 发布成绩 -> 导出归档

### 6.2 学生组队流程

加入教学班 -> 浏览已有小组 -> 创建小组或申请加入 -> 组长审批/受邀人确认 -> 形成小组 -> 组长填写本组选题 -> 系统查重 -> 教师审核 -> 完成组队与选题

### 6.3 小组提交流程

小组作业：查看作业 -> 组内准备文件 -> 任一成员上传草稿 -> 成员检查预览 -> 组长正式提交 -> 系统生成版本和回执 -> 截止前由组长撤回重交

个人作业：查看作业 -> 学生本人上传文件 -> 本人预览确认 -> 本人正式提交 -> 系统生成版本和回执 -> 截止前由本人撤回重交

### 6.4 互评流程

教师开启组内互评并配置规则 -> 学生按规则选择评价对象 -> 按量表评分并填写评语 -> 提交评价 -> 被评价者按公开范围查看评价 -> 教师复核分布与异常评价 -> 发布成绩

## 7. 详细功能需求

### 7.1 登录与课程

| 编号 | 功能 | 需求与验收标准 |
|---|---|---|
| F-01 | 登录 | 用户可使用学号/工号和密码登录；连续失败次数、限制时长等由系统安全策略配置；退出后会话失效。 |
| F-02 | 创建教学班 | 教师选择学期并填写班级名称；课程名称固定为“软件工程”；系统生成唯一班级邀请码。 |
| F-03 | 名单与成员账号 | 教师上传 XLSX/CSV 名单，XLSX 自动识别包含“学号、姓名”表头的工作表；系统先校验预览并明确提示使用学号作为初始密码，确认后以学号为账号创建新用户，不显示首次改密提示；已有账号更新姓名并加入教学班，但不覆盖原密码。教师可逐个新增、查询、修改姓名和移出教学班成员，移出不删除历史提交与成绩。 |
| F-04 | 教学班主页 | 展示进行中作业、截止时间、组队状态、互评待办和公告。 |

### 7.2 小组管理

| 编号 | 功能 | 需求与验收标准 |
|---|---|---|
| F-05 | 创建小组 | 未入组学生可创建小组，自动成为组长；名称在课程内唯一。 |
| F-06 | 浏览小组 | 学生可查看小组名称、当前人数、人数上限及是否允许申请；其他小组的选题是否展示由教师配置，隐私信息不展示。 |
| F-07 | 申请加入 | 未入组学生可申请多个小组；通过一个后，系统自动关闭其余申请。 |
| F-08 | 审批申请 | 组长可通过或拒绝；满员、申请人已入组或已过组队截止时间时不可通过。 |
| F-09 | 邀请成员 | 组长可邀请本课程未入组学生；受邀人确认后才加入；邀请可取消、拒绝和过期。 |
| F-10 | 退出与解散 | 截止前成员可退出；组长须先移交；教师可在截止后强制调整并留下日志。 |

### 7.3 选题管理

| 编号 | 功能 | 需求与验收标准 |
|---|---|---|
| F-11 | 填写本组选题 | 组长在“我的小组”中填写选题名称和说明；组员可查看但不能修改；是否展示公共选题列表由教师配置。 |
| F-12 | 选题查重 | 提交时校验同一教学班内是否已有等价选题；重复则拒绝并提示重新填写，但不泄露其他小组题目。 |
| F-13 | 审核与调整 | 教师可查看全班小组与选题，通过、驳回或要求修改；处理结果通知本组并记录原因。 |

### 7.4 作业管理与提交

| 编号 | 功能 | 需求与验收标准 |
|---|---|---|
| F-14 | 发布作业 | 教师保存草稿或发布；必填标题、说明、提交对象、截止时间；发布后通知教学班成员。 |
| F-15 | 上传附件 | 支持多文件和进度显示；拦截超限、空文件及禁用格式；失败可重试且不产生脏记录。 |
| F-16 | 草稿协作 | 小组作业的组员均可查看和上传本组草稿；个人作业仅本人可管理草稿；文件显示上传人、时间、大小，删除需二次确认。 |
| F-17 | 正式提交 | 小组作业仅组长可提交，个人作业仅学生本人可提交；确认文件清单后生成版本号、时间戳和提交回执。 |
| F-18 | 撤回重交 | 规则允许且未截止时，小组作业仅组长、个人作业仅本人可以撤回；系统保留旧版本，重新提交生成新版本。 |
| F-19 | 提交看板 | 教师按未提交、已提交、迟交筛选并查看小组、成员快照、版本和时间。 |
| F-20 | 批量下载 | 教师下载所有正式提交；系统按课程、作业和小组规范命名。 |

### 7.5 预览、评分与互评

| 编号 | 功能 | 需求与验收标准 |
|---|---|---|
| F-21 | 在线预览 | Markdown 经过安全过滤后渲染并支持代码块；PDF 和图片直接预览；Office 文件转码后预览；转码失败时提供原文件下载并显示原因。 |
| F-22 | 教师评分 | 教师输入总分或分项分数与评语；分数范围校验；保存草稿和发布分离。 |
| F-23 | 配置组内互评 | 教师为个人作业配置作品互评活动、评价维度、权重、评语最少字数、截止时间和公开时间；互评结果仅作评分参考。 |
| F-24 | 执行组内互评 | 学生选择本组其他成员的最新正式提交版本并查看授权文件后评分；服务端禁止自评、跨组访问、跨活动提交及重复有效评价。 |
| F-25 | 查看与异常处理 | 被评价者按教师配置的公开范围查看评价人的姓名、分数和评语；教师可查看全部评价、作废不当评价并填写原因，操作记入日志。 |
| F-26 | 成绩发布 | 发布前提供预览与二次确认；发布后学生仅看到授权范围内的成绩和评语。 |

### 7.6 通知与统计

| 编号 | 功能 | 需求与验收标准 |
|---|---|---|
| F-27 | 站内通知 | 作业发布、申请审批、邀请、选题结果、临近截止及成绩发布触发通知；支持已读状态。 |
| F-28 | 统计导出 | 教师可导出教学班成员、小组、选题、提交状态、评分与互评明细为 XLSX/CSV。 |
| F-29 | 审计日志 | 记录正式提交、撤回、成员调整、成绩修改和互评作废等关键操作。 |

## 8. 页面结构

### 8.1 教师端

- 登录页
- 我的教学班
- 教学班工作台
- 成员管理
- 小组与选题管理
- 作业列表 / 作业编辑
- 提交管理 / 文件预览
- 互评配置与监控
- 评分与成绩发布
- 统计导出

### 8.2 学生端

- 登录页
- 我的教学班
- 教学班首页与待办
- 小组广场
- 我的小组（含邀请、申请和本组选题）
- 作业详情 / 小组草稿 / 提交记录
- 我的互评
- 成绩与反馈

## 9. 非功能需求

| 类别 | 要求 |
|---|---|
| 性能 | 普通页面应在部署环境下保持可接受的响应时间；具体指标通过压测和试用确定。列表采用分页；大文件上传显示实时进度。 |
| 容量 | 支持首版试点规模；人数、组数和附件配额可配置，具体上限根据部署资源评估。 |
| 文件 | 单文件大小、每次提交数量和允许格式由系统或教师配置；服务端校验扩展名、MIME 和大小。 |
| 安全 | 密码加盐哈希；服务端鉴权防越权；上传文件使用随机存储名；防 SQL 注入、XSS 和 CSRF。 |
| 隐私 | 学号、姓名仅在教学必要范围展示；互评默认仅向评价人、被评价人和教师展示，公开范围由教师配置。 |
| 可靠性 | 正式提交、成绩发布等关键事务必须原子化；文件元数据与文件存储保持一致；备份频率根据部署环境制定。 |
| 兼容性 | 支持主流桌面和移动浏览器；具体版本范围在验收前确定。 |
| 可用性 | 删除、撤回、发布成绩等高影响操作需二次确认；成功后给出明确回执。 |
| 可维护性 | 关键业务状态使用枚举和状态机；接口统一错误码；关键操作记录结构化日志。 |

## 10. 核心数据对象

| 对象 | 关键字段 |
|---|---|
| 用户 | 用户 ID、学号/工号、姓名、角色、状态 |
| 课程 | 课程 ID、名称；当前课程名称为“软件工程” |
| 教学班 | 教学班 ID、课程 ID、学期、班级名称、教师、邀请码、状态、组队规则 |
| 教学班成员 | 教学班 ID、用户 ID、课程角色、加入来源、加入状态 |
| 小组 | 小组 ID、教学班 ID、名称、组长、选题、状态 |
| 小组成员 | 小组 ID、用户 ID、成员角色、加入时间、状态 |
| 入组申请/邀请 | 发起方、接收方、小组、类型、状态、过期时间 |
| 选题 | 选题 ID、教学班 ID、小组 ID、名称、标准化名称、说明、审核状态 |
| 作业 | 作业 ID、教学班 ID、标题、说明、起止时间、提交规则、互评规则 |
| 提交 | 提交 ID、作业 ID、小组 ID、版本号、状态、提交人、提交时间、成员快照 |
| 文件 | 文件 ID、业务对象、原始名、存储键、大小、类型、预览状态 |
| 互评活动 | 作业 ID、小组 ID、评价规则、公开时间、状态、截止时间 |
| 评分 | 评分人、被评价成员、分项分数、总分、评语、状态 |
| 通知 | 接收人、类型、关联对象、内容、已读状态 |
| 审计日志 | 操作者、动作、对象、变更前后、时间、IP |

## 11. 关键状态定义

### 11.1 作业状态

草稿 -> 未开始 -> 进行中 -> 已截止 -> 互评中 -> 已结束 -> 已归档

### 11.2 提交状态

未提交 -> 草稿 -> 已提交 -> 已撤回 -> 已重新提交；超过截止时间提交时额外标记“迟交”。

### 11.3 申请与邀请状态

待处理 -> 已通过 / 已拒绝 / 已取消 / 已失效。

## 12. 核心验收场景

1. 学生同时申请 A、B 两组，A 组长先通过后，学生加入 A 组，B 申请自动变为已失效。
2. 已入组学生接受其他小组邀请时，系统拒绝并说明其已属于一个小组。
3. 小组达到人数上限后，组长不能继续通过申请或邀请新成员。
4. 任一组员上传小组作业草稿后，其他组员可以查看，但只有组长能正式提交；个人作业只有学生本人能查看和提交。
5. 组长截止前撤回 V1 并提交 V2，教师默认看到 V2，同时可查看 V1 历史。
6. 截止时只有草稿未正式提交的小组，在教师看板中显示为未提交。
7. 两个小组提交按当前查重规则视为重复的选题名称时，后提交者收到重复提示；普通学生无法据此查看已占用题目，教师可查看全部选题。
8. 四人小组开启互评后，系统按教师配置允许学生选择评价对象；无论是否要求覆盖全组，都不得允许自评或跨组评价。
9. 在默认公开范围下，学生 A 评价学生 B 后，B 可看到 A 的姓名、分数和评语；同组的学生 C 不能看到该评价。
10. Markdown 中包含脚本标签或危险链接时，预览必须过滤危险内容，不能执行脚本。
11. Office 文件转码失败时不阻断正式提交，教师仍可下载原文件。
12. 名单中包含已注册学号、重复学号和缺失姓名时，导入预览分别显示“加入教学班”“重复行”“数据错误”，未经确认不写入数据。
13. 教师修改已发布成绩时必须填写原因，学生看到最新成绩，系统保留修改记录。
14. 教学班归档后，所有学生端写操作均被拒绝，但历史内容仍可查看。

## 13. 风险与应对

| 风险 | 概率 | 影响 | 应对措施 |
|---|---|---|---|
| 需求范围接近完整课堂平台 | 高 | 高 | 严格按 MVP 实现，以作业闭环为边界，不扩展签到、直播等模块。 |
| 在线预览技术复杂或格式失真 | 高 | 中 | Markdown 安全渲染、PDF/图片直接预览，Office 异步转 PDF，始终保留原文件下载。 |
| 多人同时操作导致版本冲突 | 中 | 高 | 正式提交使用事务、版本号和乐观锁，界面提示当前最新版本。 |
| 自由选择导致人情分、恶意评分或样本不均 | 高 | 高 | 实名可追溯、统一量表、展示获评数量、离群值提示；默认仅供教师参考，不直接计入成绩。 |
| 学号作为初始密码导致凭据泄露 | 高 | 高 | 导入确认时明确提示风险；不展示或导出明文密码；提供主动改密、教师重置和登录失败限制。 |
| 大文件耗尽服务器空间 | 中 | 高 | 文件大小与配额限制、对象存储、生命周期清理和监控告警。 |
| 学生通过 URL 越权查看其他作品 | 中 | 高 | 后端按课程、作业、分配关系逐层鉴权，不能依赖前端隐藏。 |
| 成绩计算规则产生争议 | 中 | 高 | 发布前明确权重和取整规则，保留原始评分与计算明细。 |

## 14. 建议开发阶段

| 阶段 | 交付内容 | 闭环目标 |
|---|---|---|
| Sprint 1 | 登录、教学班、名单导入、默认账号、基础权限 | 师生进入同一教学班空间 |
| Sprint 2 | 小组、申请邀请、选题 | 完成学生组队与选题闭环 |
| Sprint 3 | 作业发布、附件、草稿、正式提交、版本 | 完成作业收交闭环 |
| Sprint 4 | 预览、教师评分、成绩发布、导出 | 完成批改与反馈闭环 |
| Sprint 5 | 组内互评、公开范围、实名反馈、评价统计 | 完成组内成员评价闭环 |
| Sprint 6 | 通知、审计、安全测试、体验优化 | 达到课程验收条件 |

## 15. 待确认决策

以下内容不阻塞当前正式 MVP，后续扩展前再统一确认：

1. 首版按“教师导入名单并以学号作为初始账号和密码”实现；后续是否需要接入学校统一身份认证。
2. 组内互评默认仅供教师参考；若要计入个人最终成绩，需要确认获评数量不一致时的算法、最低样本数、上下限和取整规则。
3. 迟交是否允许、是否自动扣分，以及教师能否为个别小组延期。
4. 后续是否将首次修改密码改为强制，以及密码有效期等安全策略。
5. 附件最大容量、允许格式、教学班规模和数据保留年限。
6. 性能目标、浏览器版本范围和备份频率。
7. 是否需要部署到公网，还是仅进行校园网或课堂演示。

## 16. 产品价值总结

该系统不是简单地把 QQ 文件传输搬到网页，而是用课程、小组、作业、提交版本、互评任务和成绩这些结构化对象，建立可追踪的教学闭环。MVP 的判断标准是：教师无需在 QQ 中人工整理文件，学生无需依靠聊天记录确认组队和提交状态，双方均能在系统内找到唯一、明确且可审计的结果。
